/**
 * Import and export selenium-webdriver. Having a shim here will allow us to fix
 * WebDriver issues encountered at this level. This also means we can switch out
 * WebDriver libraries if better alternatives are available.
 */